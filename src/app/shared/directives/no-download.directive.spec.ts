import { NoDownloadDirective } from './no-download.directive';

describe('NoDownloadDirective', () => {
  it('should create an instance', () => {
    const directive = new NoDownloadDirective();
    expect(directive).toBeTruthy();
  });
});
